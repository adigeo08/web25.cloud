// @ts-check

export const LOG_LEVELS = {
    DEBUG: 0,
    INFO: 1,
    WARN: 2,
    ERROR: 3,
    NONE: 4
};

const IS_PRODUCTION = window.location.hostname !== 'localhost' && !window.location.hostname.startsWith('127.');
export const CURRENT_LOG_LEVEL = IS_PRODUCTION ? LOG_LEVELS.WARN : LOG_LEVELS.DEBUG;

export const PEERWEB_CONFIG = {
    EARLY_PROCESS_THRESHOLD: 0.95,
    ESSENTIAL_FILES_THRESHOLD: 0.8,
    FILE_PROGRESS_THRESHOLD_REGULAR: 0.8,
    FILE_PROGRESS_THRESHOLD_ESSENTIAL: 0.9,
    FILE_PROGRESS_THRESHOLD_MEDIA: 0.7,
    PROCESSING_TIMEOUT_BASE: 15000,
    PROCESSING_TIMEOUT_PER_MB: 2000,
    PROCESSING_TIMEOUT_PER_FILE: 500,
    PROCESSING_TIMEOUT_MIN: 15000,
    PROCESSING_TIMEOUT_MAX: 300000,
    SW_WAIT_TIMEOUT_BASE: 10000,
    SW_WAIT_TIMEOUT_PER_MB: 1000,
    SW_WAIT_TIMEOUT_MIN: 10000,
    SW_WAIT_TIMEOUT_MAX: 120000,
    SCRIPT_LOAD_TIMEOUT: 10000,
    READY_CHECK_INTERVAL: 500,
    INIT_DELAY: 100,
    INIT_RETRY_DELAY: 1000,
    FILE_TIMEOUT_BASE: 5000,
    FILE_TIMEOUT_PER_MB: 1000,
    FILE_TIMEOUT_MIN: 5000,
    FILE_TIMEOUT_MAX: 60000,
    FILE_RETRY_DELAY: 2000,
    FILE_RETRY_TIMEOUT: 15000,
    CACHE_MAX_AGE: 7 * 24 * 60 * 60 * 1000,
    MEDIA_CACHE_SIZE_THRESHOLD: 102400,
    PIECE_SIZE_THRESHOLD_16MB: 16 * 1024 * 1024,
    PIECE_SIZE_THRESHOLD_256MB: 256 * 1024 * 1024,
    PIECE_SIZE_THRESHOLD_1GB: 1024 * 1024 * 1024,
    PIECE_SIZE_16KB: 16 * 1024,
    PIECE_SIZE_32KB: 32 * 1024,
    PIECE_SIZE_256KB: 256 * 1024,
    PIECE_SIZE_1MB: 1024 * 1024
};
